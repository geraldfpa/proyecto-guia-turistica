/**
 * Template Helper tags for Editor support
 * By assigning String.raw to `html` and `css`, extensions like `es6-string-html`
 * inside VS Code will activate syntax highlighting and formatting without needing
 * any external library.
 */

export const html = String.raw;
export const css = String.raw;
